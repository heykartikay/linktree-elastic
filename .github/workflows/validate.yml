name: Validate region files

on:
  push:
    paths:
      - 'regions/**.json'
  pull_request:
    branches:
      - main

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Validate region JSON files
        run: node scripts/validate-regions.js
